pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t muppu0304/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag muppu0304/request-logger:${env.BUILD_ID} muppu0304/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push muppu0304/request-logger:${env.BUILD_ID}"
        sh "docker push muppu0304/request-logger:latest"
      }
    }
  }
}
